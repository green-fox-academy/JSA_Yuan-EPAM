import React from 'react'
import Item from './Item'

const SubmissionItem = ({items}) => (
    <ul>
        <p>Place to show submission result</p>
        {/* {items.map(item => (
            <Item {...item} />
        ))} */}
    </ul>
)

export default SubmissionItem